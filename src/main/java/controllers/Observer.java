package controllers;

/**
 * Lays out the required functionality for an Observer in the Observer Design Pattern.
 *
 * @author Samuel Thand
 */
public interface Observer {

    /**
     * Update this Observer that there has been a change of state.
     */
    void update();
}
